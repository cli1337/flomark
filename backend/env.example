# ==================================
# 🔐 DATABASE CONFIGURATION
# ==================================
# Required for production mode
# NOT required if DEMO_MODE=true (demo uses in-memory storage)

# Database Provider: mongodb, postgresql, mysql, or sqlite
DATABASE_PROVIDER=mongodb

# Database Connection String (examples below)
DATABASE_URL=mongodb://localhost:27017/flomark

# MongoDB Examples:
# DATABASE_PROVIDER=mongodb
# DATABASE_URL=mongodb://localhost:27017/flomark
# DATABASE_URL=mongodb+srv://user:password@cluster.mongodb.net/flomark

# PostgreSQL Examples:
# DATABASE_PROVIDER=postgresql
# DATABASE_URL=postgresql://user:password@localhost:5432/flomark
# DATABASE_URL=postgresql://user:password@host:5432/flomark?schema=public

# MySQL Examples:
# DATABASE_PROVIDER=mysql
# DATABASE_URL=mysql://user:password@localhost:3306/flomark

# SQLite Example:
# DATABASE_PROVIDER=sqlite
# DATABASE_URL=file:./flomark.db

# ==================================
# 🔑 JWT AUTHENTICATION
# ==================================
# Generate secure secrets with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# ==================================
# 🌐 SERVER CONFIGURATION
# ==================================
PORT=3000
BACKEND_URL=http://localhost:3000

# ==================================
# 🎭 DEMO MODE CONFIGURATION (Optional)
# ==================================
# Enable demo mode with in-memory storage
# - No MongoDB required! Uses in-memory storage
# - Creates 2 example projects automatically
# - Auto-resets every 20-30 minutes
# - Auto-login as demo@flomark.app (password: demo)
# - Perfect for landing pages and quick demos
DEMO_MODE=false

# ==================================
# 📧 EMAIL/SMTP CONFIGURATION (Optional)
# ==================================
# Required for sending invitation emails and notifications
# Leave empty to disable email functionality

# SMTP Server Settings
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# SMTP Authentication
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Email Sender Information
SMTP_FROM_NAME=Flomark
SMTP_FROM_EMAIL=noreply@flomark.com

# ==================================
# 📝 SMTP PROVIDER EXAMPLES
# ==================================

# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password (generate at: https://myaccount.google.com/apppasswords)

# Outlook/Office 365:
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@outlook.com
# SMTP_PASS=your-password

# SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# Mailgun:
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=postmaster@your-domain.mailgun.org
# SMTP_PASS=your-mailgun-password

# Amazon SES:
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password

# ==================================
# 🔒 PRODUCTION NOTES
# ==================================
# 1. Never commit the actual .env file to version control
# 2. Use strong, randomly generated JWT secrets
# 3. Use environment-specific values for BACKEND_URL
# 4. For Gmail, enable 2FA and use App Passwords
# 5. Consider using a dedicated email service (SendGrid, Mailgun, etc.) in production

